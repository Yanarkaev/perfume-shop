import { Product } from "../../../../app/types/product";

export interface ProductProps {
    // brand?: string;
    // name?: string;
    // price?: number;
    // discount?: number;
    // is_hit?: boolean;
    // is_new?: boolean;
    // img?: string;
    // mlsLeft?: number
    product: Product
    variant?: "default";
  }
  